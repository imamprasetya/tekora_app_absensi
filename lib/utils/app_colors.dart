import 'package:flutter/material.dart';

class AppColor {
  // Primary
  static const Color primary = Color(0xFF1E3A8A);
  static const Color primaryLight = Color(0xFF3B82F6);
  static const Color primarySoft = Color(0xFFEFF6FF);

  // Background
  static const Color background = Color(0xFFF5F7FB);
  static const Color card = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);

  // Border & Input
  static const Color border = Color(0xFFE5E7EB);
  static const Color inputFill = Color(0xFFF9FAFB);

  // Button
  static const Color button = primary;
  static const Color buttonText = Colors.white;

  // Shadow
  static const Color shadow = Colors.black12;

  static const secondaryText = Color(0xFF64748B);
  static const accent = Color(0xFFFEF3C7); // Warna cream badge
  static const accentText = Color(0xFFD97706); // Warna orange badge
  static const logoutBg = Color(0xFFFFE4E1);
  static const logoutText = Color(0xFFD32F2F);

  static const textGrey = Color(0xFF94A3B8);
  static const success = Color(0xFF4ADE80);
  static const danger = Color(0xFFF87171);
}
